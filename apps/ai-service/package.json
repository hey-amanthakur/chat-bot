{
  "name": "@chat-bot/ai-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "./setup-python.sh",
    "dev": ".venv/bin/uvicorn app.main:app --reload --port 8000",
    "test": ".venv/bin/pytest",
    "clean": "find . -type d -name __pycache__ -exec rm -rf {} + || true"
  }
}
